# 回归检查清单（功能冻结基线）

> 用途：功能冻结期间的回归验收标准。重构/修改代码后，按此清单逐项验证，全部通过才算该次改动成功。
> 基线：`v1.0-freeze-base`（2026-08-12 打标）

## 使用方式

1. 每次改动代码后，重启服务，在飞书里逐项测试。
2. 每项在"结果"列标记 ✅ / ❌，❌ 必须附上报错信息。
3. 清单全部 ✅ 才允许合并/继续下一步；出现 ❌ 立即回退基线并排查。

## 回归清单

| # | 功能 | 验证方法 | 结果 |
|---|------|---------|------|
| 1 | 普通对话 | 飞书发消息，收到 AI 回复卡片 | ⬜ |
| 2 | 长任务不误杀 | 让 AI 执行 scp/cargo 类长命令，10 分钟内不报"疑似死锁" | ⬜ |
| 3 | 权限门禁 | 新会话未授权时静默（仅 /auth 可申请） | ⬜ |
| 4 | 管理员绑定 | 当前主会话能收到"您已成为管理员"欢迎卡片 | ⬜ |
| 5 | /auth 申请 | 另开会话发 /auth，管理员收到授权申请卡片 | ⬜ |
| 6 | /user 管理 | /user 打开管理面板；grant/revoke/ban 子命令生效 | ⬜ |
| 7 | 限流 | 普通用户连续发 6 条，第 6 条收到限流提示 | ⬜ |
| 8 | 沙箱联动 | 管理员任务 full-access；普通用户任务 workspace-write | ⬜ |
| 9 | 定时任务创建 | /cron 面板 → 新建 → `名称 \| 规则 \| Prompt` 创建成功 | ⬜ |
| 10 | 定时任务执行 | 建 30s 倒计时任务，到期收到执行报告卡片 | ⬜ |
| 11 | 任务启停/删除 | 面板按钮 暂停/启用/删除 生效 | ⬜ |
| 12 | 插件加载 | 启动日志显示 6 个插件全部加载 | ⬜ |
| 13 | 插件面板 | /plugins 看到 6 个插件，双 Tab 可切换 | ⬜ |
| 14 | 服务器巡检 | /sysinfo 或 /health 收到巡检卡片 | ⬜ |
| 15 | 备忘录 | /note add 添加、/notes 查看、卡片删除按钮生效 | ⬜ |
| 16 | AI 记忆 | /memory 查看偏好；新会话自动注入偏好上下文 | ⬜ |
| 17 | 媒体解析 | 发图片 / Word / PDF / 音视频能解析并回复 | ⬜ |
| 18 | 项目管理 | /project 浏览/切换工作区目录 | ⬜ |
| 19 | 模型面板 | /model 弹出面板，切换后生效 | ⬜ |
| 20 | 上下文/额度 | /context、/quota 面板正常 | ⬜ |
| 21 | OTA 更新 | /update 检查版本，/update confirm 能完成热升级 | ⬜ |
| 22 | /stop | 长任务执行中发 /stop 能立即中断并清空队列 | ⬜ |
| 23 | 状态查询 | /status 显示 CPU/内存/版本 | ⬜ |
| 24 | 帮助菜单 | /help 面板按钮可点 | ⬜ |

## 自动化冒烟测试记录（2026-08-12）

> 全部走函数级验证：mock 飞书发送与数据库读写，不产生真实消息与数据变更。
> 结论：**通过** 7 组（插件加载 1 + 插件功能 6），未发现缺陷。

| 清单项 | 验证内容 | 结果 |
|---|---|---|
| #12 插件加载 | PluginManager 加载 6 插件、注册 10 命令，启动日志一致 | ✅ |
| #14 服务器巡检 | build_health_card 结构正确；/health、/sysinfo 命令分发成功 | ✅ 函数级 |
| #15 备忘录 | /note add 写入会话、/notes 列表卡片、delete_note 按钮删除 | ✅ 函数级 |
| #16 AI 记忆 | /memory 卡片；before_ai 自动注入偏好到 memory_context | ✅ 函数级 |
| #13 插件面板 | /cron 等插件命令分发与卡片构建通过；双 Tab 按钮点击待飞书验证 | 🔶 部分 |
| #9/#10/#11 定时任务 | CronEngine 会话隔离单测通过（3 项）；面板构建通过；真实触发执行报告卡片待飞书验证 | 🔶 部分 |
| #21 OTA 更新 | /update 检查路径（git fetch mock）返回"已是最新版本"卡片；confirm 热升级未测 | 🔶 部分 |

**仍待飞书端交互验证**：#1 普通对话、#2 长任务、#3 权限门禁、#4 管理员绑定、#5 /auth 申请、
#6 /user 管理、#7 限流、#8 沙箱联动、#13 面板点击、#9~#11 真实定时执行、
#17 媒体解析、#18 /project、#19 /model、#20 /context /quota、#21 confirm 热升级、
#22 /stop、#23 /status、#24 /help。

## 系统命令冒烟测试记录（2026-08-12 第二轮）

> 21 组断言全部通过。同样 mock 飞书发送 / 数据库 / codex RPC / git fetch，
> 不产生真实消息、数据变更与网络写操作。真实系统信息（pm2、磁盘、git）只读读取。

| 清单项 | 验证内容 | 结果 |
|---|---|---|
| #15 备忘录 | 系统路径 /note add / del / clear / list 全流程 | ✅ 函数级 |
| #16 AI 记忆 | /memory 系统路径卡片 + 偏好注入 | ✅ 函数级 |
| #18 项目管理 | /project 目录浏览卡片、路径设定、非法路径校验 | ✅ 函数级 |
| #19 模型面板 | /model 面板卡片构建（RPC mock 后回退静态模型表） | ✅ 函数级 |
| #20 上下文/额度 | /context、/quota 卡片构建（RPC mock） | ✅ 函数级 |
| #21 OTA 更新 | /update 检查路径：fetch mock + 本地已同步 → "已是最新版本"卡片 | ✅ 函数级 |
| #23 状态查询 | /status 卡片（真实 pm2/系统数据只读采集） | ✅ 函数级 |
| #24 帮助菜单 | /help 卡片构建 | ✅ 函数级 |
| #9/#10/#11 定时任务 | /cron 面板系统路径构建通过 | 🔶 部分 |
| 其他 | /ping、/clear、/brain、/auth、/user、pending 状态流转（笔记/记忆/项目/工作区根/定时任务创建）、未知指令兜底 | ✅ 函数级 |

## 真实端到端验证记录（2026-08-12 第三轮）

> 在运行中的机器人数据库插入 30 秒倒计时自检任务，由 CronEngine 真实触发、
> 真实调用 Codex CLI 执行并向本会话推送执行报告卡片。执行后任务行已清理，
> `cron_logs` 保留一条 success 记录作为证据。

| 清单项 | 验证内容 | 结果 |
|---|---|---|
| #10 定时任务执行 | delay 30s 真实触发：11:38:46 触发 → 11:38:51 完成，耗时 5.4s，status=success，报告卡片已推送 | ✅ 真实端到端 |
| #1 普通对话 | 本会话日常消息（怎么样了 / 继续 / 本轮验证指令）均经真实飞书链路收发处理 | ✅ 真实链路 |

## 第四轮：按钮修复 + 项目落盘验证（2026-08-12）

> 按钮分发 bug 修复（commit `21dff3e`）：插件分发分支曾拦截全部内置按钮动作，
> 已重排为内置优先、插件兜底，并补全插件面板/商店按钮处理；6 组按钮分发测试通过。
> 项目落盘在 `/tmp` 临时根目录做端到端验证，5 组全过，验证后已清理。

| 清单项 | 验证内容 | 结果 |
|---|---|---|
| #13 面板按钮 | user_choice 选项、/project 全按钮、模型切换、笔记/记忆/状态/额度、插件面板按钮分发测试 6 组全过 | ✅ 函数级 |
| #18 新建项目落盘 | 真实创建目录 + prompt.txt；冲突卡片；keep 后缀；replace 重建；cancel 切换 5 组全过 | ✅ 真实端到端 |

## 第五轮：阶段 5/6 重构后全量回归（2026-08-12）

> 架构模块化（main.py 1395→99 行、handlers + cards 分层）与工程化对齐完成后，
> 运行综合回归 4 组全过 + 定时任务真实端到端 + 服务稳定性观察。

| 清单项 | 验证内容 | 结果 |
|---|---|---|
| 系统命令 | /ping /memory /note /context /status /help /project /model /quota /auth /user /cron /update + pending 流程 | ✅ 函数级 |
| 插件 | 6 插件加载、10 命令注册、/health /light /memory 偏好注入 /note 增删 | ✅ 函数级 |
| 卡片动作 | user_choice、select_project、switch_model、cron/plugin 面板按钮分发 | ✅ 函数级 |
| 卡片构建 | 38 个卡片方法与重构前基线快照逐方法对比，零漂移 | ✅ 快照对比 |
| #10 定时任务执行 | 重构后真实 30s 任务：13:15:50 触发 → 93s 完成 success，报告卡片已推送 | ✅ 真实端到端 |
| 服务稳定性 | 重构上线后 30+ 分钟零错误、零崩溃、零异常重启 | ✅ 观察 |

## 第六轮：/stop 中断 + 定时任务生命周期（2026-08-12）

| 清单项 | 验证内容 | 结果 |
|---|---|---|
| #22 /stop | 真实子进程（独立进程组 sleep 300）注册为运行任务 + 队列积压 + 活跃 worker，/stop 后：进程被 killpg 终止、队列清空、worker 取消、回复正确 | ✅ 真实进程验证 |
| #9/#11 定时任务 | DB 级全生命周期：创建 → 进入活跃列表 → 暂停 → 启用 → 运行计数更新 → 删除 | ✅ 函数级 |

**仍待飞书端交互验证**：#2 长任务、#3 权限门禁、#4 管理员绑定、#5 /auth 申请、
#6 /user 管理、#7 限流、#8 沙箱联动、#13 面板点击、#9 面板创建定时任务（真实按钮流程）、
#17 媒体解析、#21 confirm 热升级。

## 回退方法

基线回退（放弃所有未提交改动）：

```bash
cd /Users/user/codex-feishu-bot
git checkout v1.0-freeze-base -- .
pm2 restart codex-feishu-bot
```

注意：`.env` 和数据库文件不纳入版本管理，回退不会影响凭证与已存数据。

## 冻结期纪律

- 只修 bug，不加新功能；新需求记入"冻结后待办"。
- 每次改动小步走：一次只拆/改一个模块，改完立即跑对应回归项。
- 全量回归通过后才允许 pm2 重启上线新代码。
